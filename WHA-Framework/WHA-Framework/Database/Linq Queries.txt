
Query Data :
var query = from it in context.Companies
            orderby it.CompanyID
            select it



Convert.ToInt64(from b in db.tblTransactionTypes where b.TransactionTypeName == tsy.TransType select b.TransactionTypeId),

var  tes = db.tblTransactions.FirstOrDefault(p => p.TransID == id);


